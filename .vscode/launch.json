{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python デバッガー: Test",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/src/test.py",
            "console": "integratedTerminal",
            "args": "${command:pickArgs}"
        }
    ]
}